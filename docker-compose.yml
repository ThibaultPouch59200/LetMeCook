services:
  letmecook:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3004:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      SITE_PASSWORD: ${SITE_PASSWORD:-demo123}
    restart: unless-stopped